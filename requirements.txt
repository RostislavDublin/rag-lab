# RAG Lab Dependencies
# 
# This file kept for backward compatibility and local development.
# Production deployments should use:
#   - requirements-base.txt (always needed)
#   - requirements-optional.txt (optional: for on-premise embeddings/reranking)
#
# For production with Vertex AI only: pip install -r requirements-base.txt

# Include all base dependencies
-r requirements-base.txt

# Optional: On-premise embeddings and reranking (sentence-transformers)
# Uncomment for vendor-independent deployment
-r requirements-optional.txt
html2text>=2024.2.26  # HTML to Markdown conversion (preserves structure like PDF)
